# Tasks

## 1. C++ CLI 驱动（CSV 输入 / CSV 输出）

- [x] 1.1 新增 CLI 入口文件骨架（按 `--engine/--in/--out` 参数分派），验证 `--help` 可列出四类引擎与参数说明
- [x] 1.2 实现最小 CSV 写出工具（正确处理逗号、引号转义、换行与表头），验证含逗号/引号的字段可被 DuckDB `read_csv_auto` 正确还原
- [x] 1.3 把 CLI 入口加入构建配置（CMake 目标 + `build_and_run.sh`），验证构建产出可执行文件且既有 4 个基准程序仍正常编译运行
- [x] 1.4 确认未改动既有引擎源码与基准程序，验证 `git diff` 仅显示新增 CLI 文件与构建配置的新增行
- [x] 1.5 验证构建 CLI 后既有基准输出与断言不变（交付精度、ITP/IOP、替代料三级三个基准均仍 PASS）
- [x] 1.6 验证 CLI 可直接以既有 `data/sample/` 与 `data/` 目录作为 `--in` 参数完成计算，无需任何格式转换

## 2. C++ CLI 各引擎的输出契约

- [x] 2.1 实现 `delivery` 分支：读取 `parts/bom/atp_supply/capacity` 及查询参数，调用 `promise_delivery_date`，写出 `delivery_result.csv`，验证需求 30 件 @ Day3 得到承诺 Day 3、预留 3 工时
- [x] 2.2 实现 `delivery_steps.csv` 逐层 BOM/供给/产能试算与回滚步骤输出，验证步骤可重建"为什么承诺 Day 3"与回滚发生位置
- [x] 2.3 实现 `itp` 分支：调用 `generate_itp_master_allotments`，写出 `itp_allotments.csv` 含四维约束键与配额，验证配额等于该键需求之和乘缓冲区系数
- [x] 2.4 实现 `iop` 分支逐单明细 `iop_orders.csv`（含 status 与 reason）与 `iop_summary.csv`，验证对拍得到下派 3 / 阻断 1 且明细计数与算子聚合结果完全相等
- [x] 2.5 实现 `substitution` 分支：按替代料类分派调用 `allocate_class1/2/3`，写出 `substitution_allocations.csv` 与 `substitution_decisions.csv`，验证一类选中 ID 1、三类分配 10（⌈6/5⌉×5）且安全库存 10 未被侵占
- [x] 2.6 统一约束键计算位置：所有 `family_id/cust_group_id/region_id` 均由 CLI 侧计算并写入 CSV，验证 Node 侧代码中不存在这些键的计算逻辑
- [x] 2.7 实现 CLI 错误语义（输入目录缺失、必需 CSV 缺失、参数非法、引擎类型未知），验证非零退出码且 stderr 有明确原因

## 3. CLI 契约验收与回归

- [x] 3.1 为 CLI 编写回归脚本，对四个引擎分别执行并校验输出 CSV 的表头、行数与关键数值，验证脚本全绿
- [x] 3.2 用 CLI 输出复现既有基准的三项结论（承诺 Day 3 / 下派 3 阻断 1 / 替代料分配 10），验证与 `benchmarks/*` 和 `verifier` 对账一致
- [x] 3.3 编写 CLI 契约文档（输入 CSV、输出 CSV、退出码、约束键约定），验证文档中的示例命令可原样执行成功

## 4. 测试场景管理

- [x] 4.1 实现场景扫描与发现（遍历场景根目录，含根目录本身），验证既有 `data/sample/` 与 `data/` 被识别为两个场景
- [x] 4.2 实现场景列表接口，返回场景标识、显示名与各数据集行数概览，验证与源文件行数一致
- [x] 4.3 实现场景完整性校验（七类数集缺失/不可读、按引擎标注可否运行），验证缺失场景标注正确且不误报完整场景
- [x] 4.4 实现场景详情接口（按引擎分别标明运行条件与缺失项），验证每种缺失组合的标注结果
- [x] 4.5 实现新建场景（写入可写场景根，默认不改动仓库既有数据），验证新场景出现在列表中且既有场景目录内容不变
- [x] 4.6 实现场景重名冲突处理，验证以已存在标识创建时返回明确冲突错误
- [x] 4.7 实现为场景提供/替换数据集并触发重新校验，验证替换后浏览视图反映新内容与行数
- [x] 4.8 实现空场景根的处理（返回空列表与创建指引，不报错退出），验证空目录下服务仍可启动

## 5. DuckDB 持久与分析层

- [x] 5.1 创建 `ipc-demo/` 目录结构与忽略规则（`.data/`、运行工作目录、`node_modules/`），验证 `git status` 不显示这些路径
- [x] 5.2 封装 DuckDB 连接与配置解析（`DUCKDB_PATH`、`SCENARIO_ROOT`、`PORT`、`ENGINE_BIN`、`ENGINE_TIMEOUT_MS`），验证自定义路径生效
- [x] 5.3 编写 schema 初始化/迁移：`scenarios`、7 张带 `scenario_id` 的基础数据集表、`dataset_imports`、`engine_runs`、`run_artifacts`、9 张结果明细表，验证空库启动后表清单与设计一致
- [x] 5.4 实现按场景的输入数据集导入（`read_csv_auto`），验证导入行数与场景 CSV 一致且每行带正确 `scenario_id`
- [x] 5.5 实现导入幂等（按 `scenario_id` 先删后插），验证同一场景重复导入后各表行数不变
- [x] 5.6 实现多场景共存导入，验证按场景筛选查询时各场景行数与各自源文件独立一致
- [x] 5.7 在 `dataset_imports` 记录场景、数据集类别、行数、源路径与导入时间，验证可查询最近一次导入元数据
- [x] 5.8 实现输入数据的业务类别查询（需求/供给/资源/计划供给/物料主数据/产品结构/替代料组），验证每类查询返回预期字段
- [x] 5.9 实现运行元数据与结果明细写入（含失败/超时运行），验证按 `run_id` 可原样取回且带 `scenario_id`
- [x] 5.10 实现 `run_artifacts` 归档输入/输出 CSV 原文，验证可检索任意历史运行的原始 CSV
- [x] 5.11 实现历史查询接口（按场景/引擎类型列摘要、按 `run_id` 取详情），验证未知 `run_id` 返回明确的未找到错误
- [x] 5.12 实现大数据集导入路径并报告导入耗时与总行数，验证小场景导入不被强制加载大数据

## 6. 计划透视（时间分桶关键指标）

- [x] 6.1 实现时间范围推导（需求 `due_day`、供给 `available_day`、产能 `day` 的并集区间），验证生成的连续天桶覆盖数据实际范围
- [x] 6.2 实现供给族指标（在手、在途 SR、计划订单、累计计划可用量），验证各指标按天取值与 `atp_supply` 一致
- [x] 6.3 实现需求族指标（主计划独立需求、执行需求、毛需求），验证按 `due_day` 分桶且两种需求分别成行
- [x] 6.4 实现资源族指标（可用产能、已占用产能、剩余产能），验证已占用与交付承诺结果一致且超载日剩余量为负
- [x] 6.5 实现平衡族指标（已承诺量、配额 vs 消耗、阻断量、缺口），验证配额与阻断量与 ITP/IOP 结果一致
- [x] 6.6 实现透视查询接口：返回「指标 × 天」规整结构，支持按场景与物料/工作中心/客户组/大区筛选，验证筛选后各行重算正确
- [x] 6.7 实现单元格下钻接口，返回构成该值的底层记录，验证下钻明细求和等于单元格值
- [x] 6.8 保证空值语义：SQL 聚合不使用 `COALESCE(...,0)` 抹平缺失，验证无计划日返回空值而非 0
- [x] 6.9 为透视编写一致性测试（产能对齐交付承诺、配额/阻断对齐 ITP/IOP），验证全部通过

## 7. Node 编排层（调用 C++ 引擎）

- [x] 7.1 实现运行工作目录管理（每次运行独立 `in/out` 目录），验证并发两次运行不互相覆盖文件
- [x] 7.2 实现以场景为输入的运行准备：默认可直接以场景目录作为 CLI 输入，验证运行后场景目录文件内容不变
- [x] 7.3 实现场景参数覆盖路径（请求显式指定物料/交期/数量/优先级时写入运行时副本），验证覆盖不影响场景原文件
- [x] 7.4 实现子进程调用（`spawn` C++ 可执行文件、传参、捕获 stdout/stderr 与退出码、施加超时），验证正常/失败/超时三种路径均可复现
- [x] 7.5 实现成功判定（退出码 0 且输出 CSV 存在且必需列可解析），验证产物缺失或列不全会判为失败而非部分成功
- [x] 7.6 实现失败处置：可执行文件缺失（给出构建指引）、非零退出码（附退出码与 stderr 摘要）、超时（说明时限）、输出格式错误，验证四类错误响应语义各不相同
- [x] 7.7 实现输出 CSV 解析与入库，验证明细行数与输出 CSV 行数一致
- [x] 7.8 记录 Node 端到端耗时与 C++ 自报耗时，验证性能数据可分别取到
- [x] 7.9 对并发子进程数设上限，验证超过上限时排队而非无限制并发

## 8. 后端 HTTP API

- [x] 8.1 实现 `GET /health`，分别标明 DuckDB 连接、场景是否可用、C++ 可执行文件是否存在，验证三种前置缺失各自可被识别
- [x] 8.2 实现场景相关端点（列表/详情/新建/数据集提供），验证与第 4 组的功能测试一致
- [x] 8.3 实现输入数据浏览端点（按业务类别与场景查询、原始 CSV 内容获取），验证返回内容与场景文件一致
- [x] 8.4 实现交付承诺端点（触发 CLI + 入 DuckDB + 返回结论与回放步骤），验证合法请求返回承诺结论、非法输入返回 4xx 且不启动子进程
- [x] 8.5 实现 ITP/IOP 端点（配额、逐单下派/阻断、聚合指标），验证阻断订单记录含约束键与原因
- [x] 8.6 实现替代料端点（三类分支、选中依据、安全库存边界），验证不可分配场景返回明确业务结论而非错误
- [x] 8.7 实现计划透视端点与单元格下钻端点，验证与第 6 组的一致性与筛选用例一致
- [x] 8.8 实现运行历史与详情端点（含产物检索），验证可按 `run_id` 无重算地取回该次运行全部数据
- [x] 8.9 为 API 编写集成测试覆盖规格场景（含引擎失败与业务失败的区别），验证全部通过

## 9. 前端可视化

- [x] 9.1 搭建 SPA 骨架与路由（场景 / 计划透视 / 交付承诺 / ITP-IOP / 替代料 / 性能），验证各视图均可导航打开
- [x] 9.2 实现场景选择与全局上下文，验证切换场景后所有视图数据随之切换且不混入其他场景
- [x] 9.3 实现场景浏览视图：按需求/供给/资源/计划供给/物料主数据/产品结构/替代料组分类的表格，标明行数与来源，验证与源数据一致
- [x] 9.4 实现原始 CSV 内容查看，验证展示内容与场景文件原文一致
- [x] 9.5 实现场景输出数据浏览与场景内运行切换，验证可查看该场景历次运行的输出
- [x] 9.6 实现缺失数据集与不可运行引擎的提示，验证不再以空白表格呈现
- [x] 9.7 实现计划透视图网格（行=指标，列=天，连续时间桶、横向滚动），验证渲染结构正确
- [x] 9.8 实现指标按供给/需求/资源/平衡四族分组与折叠展开，验证分组内容与规格一致
- [x] 9.9 实现透视筛选（物料/工作中心/客户组/大区）与按天重算，验证筛选结果与后端一致
- [x] 9.10 实现空值与零值区分渲染（空值显示为空白），并单测该行为
- [x] 9.11 实现超载与缺口的单元格视觉强调，验证负剩余产能与缺口日可被定位
- [x] 9.12 实现单元格下钻明细弹层，验证明细求和等于单元格值
- [x] 9.13 实现场景输入与运行提交（运行中状态、校验错误保留、编排失败可读提示与保留参数）
- [x] 9.14 实现交付承诺视图：BOM 层级、库存/产能水位、请求 vs 承诺日时间轴、缺口与回滚标识
- [x] 9.15 实现 ITP/IOP 视图：四维约束键配额 vs 消耗对比、临近/达到上限区分、阻断订单列表与聚合指标
- [x] 9.16 实现替代料视图：按类别的配额比例/历史量/本次分配、安全库存保护带与可分配部分区分、选中成员高亮与依据
- [x] 9.17 实现性能与历史视图：端到端耗时与 C++ 自报耗时分别展示、处理条数与吞吐量、历史运行对比图表、点击历史项加载对应 `run_id`
- [x] 9.18 实现客户端回放控制（前进/后退/自动播放/暂停/重置），验证与后端返回的步骤序列逐帧对应
- [x] 9.19 为关键交互编写组件测试，覆盖可承诺/不可承诺、有阻断/无阻断、可分配/不可分配、引擎失败、空值/零值等分支

## 10. 引导、配置与文档

- [x] 10.1 实现一条命令启动脚本（依赖检查 → 构建 C++ CLI → 建库 → 发现场景并导入输入数据 → 启动前后端），验证干净检出下一条命令可打开界面
- [x] 10.2 实现已存在数据库与已构建 CLI 时的重复启动路径，验证既存数据不被清空且启动成功
- [x] 10.3 确保默认仅绑定 `127.0.0.1`，验证从非回环地址无法连接
- [x] 10.4 在 `ipc-core-benchmark/data/demo/` 下新增覆盖四个引擎的样例测试场景（每个场景一个自包含子目录 + 完整七类数据集），验证首次启动后场景列表与各视图均有可展示数据，且 `data/` 根与 `data/sample/` 内容不变
- [x] 10.5 校验 `data/demo/` 预设场景复现基准结论（交付：承诺 Day 3 / 预留 3 工时；ITP/IOP：下派 3 阻断 1；替代料：分配 10 且安全库存 10 未侵占），验证 CLI 输出与界面呈现同基准一致
- [x] 10.6 提供 `npm run import:full` 大数据集导入入口，验证可导入并报告耗时与总行数
- [x] 10.7 编写 `ipc-demo/README.md`：启动、C++ 构建、场景组织与创建约定（含七类数据集列契约）、数据导入、配置项、CLI 契约与 API 契约，验证按文档可独立运行、可直接调用 CLI、可自行建场景

## 11. 端到端验收

- [x] 11.1 在干净环境执行完整流程（构建 CLI → 启动 → 选场景 → 浏览输入 → 计划透视 → 四引擎运行 → 回放 → 历史对比），验证与规格中所有场景相符
- [x] 11.2 运行全部测试（CLI 回归、场景管理、透视一致性、后端集成、前端组件），验证全部通过且基准结论一致
- [x] 11.3 确认既有 C++ 引擎/头文件/基准/数据未被修改，验证 `git diff` 中这些路径无改动
